version: '3'
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: frontend
    ports:
      - '3000:3000'
  backend:
    build:
      context: .
      dockerfile: Dockerfile
      target: backend
    command: npm run dev
    ports:
      - '3333:3333'